using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class InimigoFracoProximo : MonoBehaviour
{
    //propriedades

    //Target State: Caçar

    //metodos

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }

    public void Action(){
        //sem acao
    }

    public bool isTriggered(){
        //se houer algum inimigo com pouca vida mais baixa que o atual
            //return true
        return false;
    }
}
